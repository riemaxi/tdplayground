package tdpnet;

public class Codec {

    // Constructor
    public Codec() {
        // Initialize the Codec object if necessary
    }

    // Encode method
    public Object encode(Object data) {
        // Simply returns the data as-is, can be modified to perform actual encoding
        return data;
    }

    // Decode method
    public Object decode(Object data) {
        // Simply returns the data as-is, can be modified to perform actual decoding
        return data;
    }
}
